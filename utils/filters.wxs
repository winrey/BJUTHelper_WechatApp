var filters = {
  toFix: function (value) {
    var num = parseFloat(value);
    if (isNaN(num)) {
      return false;
    }
    return num.toFixed(2)//此处2为保留两位小数
  }
}
module.exports = {
  toFix: filters.toFix
}